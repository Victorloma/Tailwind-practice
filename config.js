const colors = require('taiwindcss/colors');

module.exports = {
    theme: {
        extend: {
            colors: {
                fuchia: colors.fuchsia,
            },
        },
    },
    variants: {},
    plugins: [],
}